{
  "name": "blog",
  "version": "1.0.0",
  "type": "module",
  "main": "index.js",
  "license": "MIT",
  "scripts": {
    "docs:dev": "vitepress dev docs",
    "docs:build": "vitepress build docs",
    "docs:package": "vitepress build docs && node scripts/compress-images.js && tar -czf blog.tar.gz -C docs/.vitepress/dist ."
  },
  "devDependencies": {
    "sharp": "^0.34.5",
    "vitepress": "^1.6.4"
  }
}
